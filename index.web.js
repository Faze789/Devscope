import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('DepScope', () => App);
AppRegistry.runApplication('DepScope', {
  rootTag: document.getElementById('root'),
});
